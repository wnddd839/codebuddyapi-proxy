# Prefer copying go-codebuddy/.env.example next to the binary, or just run the
# binary once — it auto-loads nearby .env and can generate a gateway API key.
#
# Example:
#   cp go-codebuddy/.env.example .env
#   ./go-codebuddy/releases/codebuddy-proxy-windows-amd64.exe
