bin/
*.exe
*.exe~
*.test
*.out
.env
.env.gz
*.log
.DS_Store
node_modules/
go-codebuddy/bin/
go-codebuddy/.env

# private deploy notes / smoke (never push)
docs/private/

# local agent / IDE scratch
.cline/
.omo/
.idea/
.vscode/

# accidental literal home / temp artifacts
~/
preview.html
admin-models.json
opencode-config.json

# prebuilt binaries live on GitHub Releases, not in git
go-codebuddy/releases/codebuddy-proxy-*
go-codebuddy/releases/SHA256SUMS.txt
go-codebuddy/releases/*.exe

# temp clones / research scratch
.tmp/

# --- workspace multi-project boundaries ---
# Outer folder may later hold both go-codebuddy and go-cursor.
# GitHub remotes for CodeBuddy must NOT publish the Cursor module.
go-cursor/

# Local-only agent/review artifacts (never push)
.local/
docs/review/
go-codebuddy/docs/review/

# Agent skills / Pi prompts stay on disk for local agents; do not publish
go-codebuddy/.agents/
